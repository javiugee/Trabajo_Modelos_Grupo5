---
title: "Práctica: Modelos de Regresión para Predicción de Notas Finales"
subtitle: "Grado en Ciencia e Ingeniería de Datos - URJC"
author:
  - name: "Javier Ugena García"
  - name: "Soraya Burgos Modrego"
  - name: "Elba Jares Darriba"
date: today
lang: es
format:
  html:
    toc: true
    toc-depth: 4
    toc-title: "Índice de Contenidos"
    toc-float: true
    number-sections: true
    theme: cosmo
    highlight-style: github
    code-fold: true
    code-tools: true
    code-summary: "Mostrar código"
    fig-width: 9
    fig-height: 6
    fig-align: center
    embed-resources: true
    df-print: kable
    css: styles.css
execute:
  echo: true
  warning: false
  message: false
  cache: false
editor: visual
---

```{css, echo=FALSE}
/* Custom styles for the report */
body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  font-size: 15px;
  line-height: 1.7;
  color: #2c3e50;
}

h1, h2, h3, h4 {
  color: #1a252f;
  font-weight: 600;
}

.callout-note {
  border-left: 4px solid #3498db;
}

.callout-tip {
  border-left: 4px solid #2ecc71;
}

.callout-warning {
  border-left: 4px solid #e67e22;
}

.callout-important {
  border-left: 4px solid #e74c3c;
}

table {
  font-size: 13px;
}

.badge-section {
  display: inline-block;
  background: #2c3e50;
  color: white;
  padding: 2px 10px;
  border-radius: 12px;
  font-size: 12px;
  margin-bottom: 8px;
}
```

------------------------------------------------------------------------

# Introducción y Descripción del Problema

## Contexto del Estudio

Este informe analiza el **rendimiento académico de estudiantes de secundaria** en Portugal, utilizando el conocido dataset *Student Performance* disponible en el [UCI Machine Learning Repository](https://archive.ics.uci.edu/dataset/320/student+performance). El dataset fue recopilado por Cortez y Silva (2008) mediante cuestionarios escolares y registros académicos en dos escuelas portuguesas: **Gabriel Pereira (GP)** y **Mousinho da Silveira (MS)**.

El objetivo principal de esta práctica es construir y evaluar modelos de regresión que permitan **predecir la nota final (G3)** de los estudiantes en la asignatura de **Portugués**, a partir de una amplia variedad de variables sociodemográficas, familiares y académicas.

## Dataset Utilizado

Se trabaja con el fichero `student-por.csv`, correspondiente a la asignatura de **Lengua Portuguesa**, que contiene **649 observaciones** y **33 variables**. Se ha elegido este dataset por su mayor tamaño muestral respecto al de Matemáticas (395 obs.), lo que proporciona mayor robustez estadística para la modelización.

::: callout-note
**Variables objetivo:**

-   **G3** (variable continua): Nota final del curso, de 0 a 20 → usada en regresión lineal.
-   **G3_bin** (variable binaria): Aprobado (G3 ≥ 10) / Suspendido (G3 \< 10) → usada en regresión logística.
:::

## Estructura del Informe

El análisis sigue la siguiente estructura:

1.  Preparación de datos y análisis exploratorio (EDA)
2.  Regresión lineal simple
3.  Regresión lineal múltiple (selección de variables, regularización, no linealidades)
4.  Regresión logística (target binarizado)
5.  Comparación y evaluación de modelos
6.  Conclusiones y trabajo futuro

------------------------------------------------------------------------

# Preparación de Datos y Análisis Exploratorio

## Carga de Librerías y Datos

```{r setup-libraries}
# ── Librerías de manipulación y visualización ──────────────────────────────
library(tidyverse)       # manipulación y visualización general
library(ggplot2)         # gráficos
library(GGally)          # pairs plots
library(corrplot)        # correlogramas
library(knitr)           # tablas
library(kableExtra)      # tablas mejoradas

# ── Librerías de modelado ──────────────────────────────────────────────────
library(caret)           # partición, entrenamiento y validación
library(glmnet)          # regularización Lasso/Ridge/ElasticNet
library(leaps)           # selección de subconjuntos
library(MASS)            # stepwise AIC/BIC
library(car)             # VIF, diagnóstico
library(lmtest)          # tests de hipótesis sobre residuos
library(nortest)         # test de normalidad Anderson-Darling

# ── Librerías de evaluación y diagnóstico ─────────────────────────────────
library(pROC)            # curvas ROC
library(ResourceSelection) # Hosmer-Lemeshow
library(gridExtra)       # grid de gráficos
library(ggfortify)       # autoplot para lm
library(broom)           # tidy model outputs
library(performance)     # check_model
library(see)             # visualización performance

# Semilla para reproducibilidad
set.seed(42)
```

```{r load-data}
# ── Carga del dataset de Portugués ────────────────────────────────────────
student_por <- read.table("student-por.csv", sep = ";", header = TRUE,
                          stringsAsFactors = TRUE)

cat("Dimensiones del dataset:", dim(student_por), "\n")
cat("Variables:", ncol(student_por), "\n")
cat("Observaciones:", nrow(student_por), "\n")
```

## Inspección Inicial

```{r initial-inspection}
# Vista general de la estructura
glimpse(student_por)
```

```{r summary-stats}
# Resumen estadístico completo
summary(student_por) |>
  kable(caption = "Resumen estadístico de todas las variables") |>
  kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
                font_size = 12, full_width = FALSE) |>
  scroll_box(width = "100%", height = "400px")
```

## Valores Ausentes y Calidad de los Datos

```{r missing-values}
# Análisis de valores ausentes
missing_df <- data.frame(
  Variable    = names(student_por),
  Tipo        = sapply(student_por, class),
  NA_Count    = sapply(student_por, function(x) sum(is.na(x))),
  NA_Pct      = round(sapply(student_por, function(x) mean(is.na(x))) * 100, 2)
)

missing_df |>
  filter(NA_Count > 0) |>
  kable(caption = "Variables con valores ausentes", row.names = FALSE) |>
  kable_styling(bootstrap_options = c("striped", "hover"))

cat("Total de valores ausentes:", sum(is.na(student_por)), "\n")
cat("→ El dataset está completo. No se requiere imputación.\n")
```

## Ingeniería de Variables

```{r feature-engineering}
# ── Preprocesamiento ───────────────────────────────────────────────────────
student <- student_por |>
  mutate(
    # Variable objetivo binarizada para regresión logística
    G3_bin = factor(ifelse(G3 >= 10, "Aprobado", "Suspendido"),
                    levels = c("Suspendido", "Aprobado")),

    # Promedio de notas parciales
    G_avg  = (G1 + G2) / 2,

    # Consumo total de alcohol (laboral + fin de semana)
    Alc_total = Dalc + Walc,

    # Educación media de los padres
    ParentEdu = (Medu + Fedu) / 2,

    # Recodificación de variables ordinales como numéricas (ya lo son)
    # Aseguramos que las binarias sean factores
    across(c(school, sex, address, famsize, Pstatus,
             schoolsup, famsup, paid, activities, nursery,
             higher, internet, romantic), as.factor),

    # Variables ordinales como ordered factors para análisis
    studytime_f = factor(studytime, levels = 1:4,
                         labels = c("<2h","2-5h","5-10h",">10h"), ordered = TRUE),
    traveltime_f = factor(traveltime, levels = 1:4,
                          labels = c("<15min","15-30min","30-60min",">1h"), ordered = TRUE)
  )

cat("Dataset procesado:\n")
cat(" - Observaciones:", nrow(student), "\n")
cat(" - Variables totales:", ncol(student), "\n")
cat("\nDistribución de G3_bin:\n")
print(table(student$G3_bin))
cat("\nTasa de aprobados:", round(mean(student$G3 >= 10) * 100, 1), "%\n")
```

## Partición Train / Validation / Test (60/20/20)

```{r data-split}
# ── Partición estratificada 60 / 20 / 20 ──────────────────────────────────
n <- nrow(student)

# Primera división: 80% train+val — 20% test
idx_test  <- createDataPartition(student$G3, p = 0.20, list = FALSE)
test_data <- student[idx_test, ]
tv_data   <- student[-idx_test, ]

# Segunda división: 75% train — 25% val (del 80% restante → 60% y 20% del total)
idx_val   <- createDataPartition(tv_data$G3, p = 0.25, list = FALSE)
val_data  <- tv_data[idx_val, ]
train_data <- tv_data[-idx_val, ]

cat("╔══════════════════════════════════════╗\n")
cat("║       PARTICIÓN DE DATOS             ║\n")
cat("╠══════════════════════════════════════╣\n")
cat(sprintf("║  Train:      %3d obs  (%.0f%%)          ║\n",
            nrow(train_data), nrow(train_data)/n*100))
cat(sprintf("║  Validation: %3d obs  (%.0f%%)          ║\n",
            nrow(val_data),   nrow(val_data)/n*100))
cat(sprintf("║  Test:       %3d obs  (%.0f%%)          ║\n",
            nrow(test_data),  nrow(test_data)/n*100))
cat(sprintf("║  Total:      %3d obs  (100%%)          ║\n", n))
cat("╚══════════════════════════════════════╝\n")

# Verificación de distribuciones similares
cat("\nMedia de G3 por partición:\n")
cat(sprintf("  Train: %.2f | Val: %.2f | Test: %.2f\n",
            mean(train_data$G3), mean(val_data$G3), mean(test_data$G3)))
```

## Análisis Exploratorio de Datos (EDA)

### Variable Objetivo: G3

```{r eda-target}
p1 <- ggplot(student, aes(x = G3)) +
  geom_histogram(aes(y = after_stat(density)), bins = 21,
                 fill = "#2c3e50", color = "white", alpha = 0.85) +
  geom_density(color = "#e74c3c", linewidth = 1.2) +
  geom_vline(xintercept = mean(student$G3), linetype = "dashed",
             color = "#3498db", linewidth = 1) +
  geom_vline(xintercept = median(student$G3), linetype = "dotted",
             color = "#2ecc71", linewidth = 1) +
  annotate("text", x = mean(student$G3) + 0.5, y = 0.13,
           label = paste0("Media: ", round(mean(student$G3), 1)),
           color = "#3498db", hjust = 0, size = 3.5) +
  annotate("text", x = median(student$G3) - 0.5, y = 0.11,
           label = paste0("Mediana: ", median(student$G3)),
           color = "#2ecc71", hjust = 1, size = 3.5) +
  labs(title = "Distribución de la Nota Final (G3)",
       x = "Nota Final (G3)", y = "Densidad") +
  theme_minimal(base_size = 13) +
  theme(plot.title = element_text(face = "bold"))

p2 <- ggplot(student, aes(x = G3_bin, fill = G3_bin)) +
  geom_bar(alpha = 0.85, color = "white") +
  geom_text(stat = "count", aes(label = after_stat(count)),
            vjust = -0.5, fontface = "bold", size = 4) +
  scale_fill_manual(values = c("Suspendido" = "#e74c3c", "Aprobado" = "#2ecc71")) +
  labs(title = "Distribución Aprobados/Suspendidos",
       x = "", y = "Número de estudiantes", fill = "") +
  theme_minimal(base_size = 13) +
  theme(plot.title = element_text(face = "bold"), legend.position = "none")

p3 <- ggplot(student, aes(sample = G3)) +
  stat_qq(color = "#2c3e50", alpha = 0.6) +
  stat_qq_line(color = "#e74c3c", linewidth = 1) +
  labs(title = "Q-Q Plot de G3",
       x = "Cuantiles teóricos", y = "Cuantiles observados") +
  theme_minimal(base_size = 13) +
  theme(plot.title = element_text(face = "bold"))

p4 <- ggplot(student, aes(x = "", y = G3)) +
  geom_boxplot(fill = "#3498db", alpha = 0.7, outlier.color = "#e74c3c",
               outlier.size = 2) +
  geom_jitter(width = 0.1, alpha = 0.2, color = "#2c3e50") +
  labs(title = "Boxplot de G3", x = "", y = "Nota Final") +
  theme_minimal(base_size = 13) +
  theme(plot.title = element_text(face = "bold"))

grid.arrange(p1, p2, p3, p4, ncol = 2)
```

```{r eda-target-stats}
# Estadísticos descriptivos de G3
cat("=== Estadísticos de G3 ===\n")
cat(sprintf("Media:          %.3f\n", mean(student$G3)))
cat(sprintf("Mediana:        %.3f\n", median(student$G3)))
cat(sprintf("Desv. estándar: %.3f\n", sd(student$G3)))
cat(sprintf("Asimetría:      %.3f\n", e1071::skewness(student$G3)))
cat(sprintf("Curtosis:       %.3f\n", e1071::kurtosis(student$G3)))
cat(sprintf("Mín - Máx:      %d - %d\n", min(student$G3), max(student$G3)))

# Test de normalidad
sw_test <- shapiro.test(student$G3)
ad_test <- nortest::ad.test(student$G3)
cat(sprintf("\nTest Shapiro-Wilk:       W = %.4f, p-valor = %.4e\n",
            sw_test$statistic, sw_test$p.value))
cat(sprintf("Test Anderson-Darling:   A = %.4f, p-valor = %.4e\n",
            ad_test$statistic, ad_test$p.value))
```

::: callout-note
**Interpretación:** La distribución de G3 es aproximadamente normal con ligera asimetría negativa, concentrándose las notas en el rango 10-14. El test de Shapiro-Wilk confirma una leve desviación de la normalidad estricta (esperable en muestras grandes), pero la forma general es suficientemente normal para aplicar regresión lineal.
:::

### Variables Numéricas: Distribuciones y Correlaciones

```{r eda-numeric}
# Variables numéricas continuas/ordinales relevantes
num_vars <- c("age", "Medu", "Fedu", "traveltime", "studytime", "failures",
              "famrel", "freetime", "goout", "Dalc", "Walc", "health",
              "absences", "G1", "G2", "G3")

num_data <- student[, num_vars]

# Correlograma
cor_matrix <- cor(num_data, use = "complete.obs")

corrplot(cor_matrix,
         method = "color",
         type   = "upper",
         order  = "hclust",
         tl.cex = 0.8,
         tl.col = "black",
         addCoef.col = "black",
         number.cex  = 0.65,
         col = colorRampPalette(c("#e74c3c", "white", "#2c3e50"))(200),
         title = "Matriz de Correlaciones — Variables Numéricas",
         mar   = c(0, 0, 1.5, 0))
```

```{r eda-correlations-table}
# Top correlaciones con G3
cor_g3 <- data.frame(
  Variable    = rownames(cor_matrix),
  Correlacion = cor_matrix[, "G3"]
) |>
  filter(Variable != "G3") |>
  arrange(desc(abs(Correlacion))) |>
  mutate(
    Correlacion = round(Correlacion, 3),
    Magnitud    = case_when(
      abs(Correlacion) >= 0.5 ~ "Alta",
      abs(Correlacion) >= 0.3 ~ "Moderada",
      abs(Correlacion) >= 0.1 ~ "Baja",
      TRUE                    ~ "Muy baja"
    )
  )

cor_g3 |>
  kable(caption = "Correlaciones de las variables numéricas con G3",
        col.names = c("Variable", "Correlación con G3", "Magnitud")) |>
  kable_styling(bootstrap_options = c("striped", "hover"), full_width = FALSE) |>
  row_spec(which(abs(cor_g3$Correlacion) >= 0.5), background = "#d5f5e3") |>
  row_spec(which(abs(cor_g3$Correlacion) >= 0.3 & abs(cor_g3$Correlacion) < 0.5),
           background = "#fef9e7")
```

### Variables Categóricas vs G3

```{r eda-categorical, fig.height=10}
cat_vars <- c("school", "sex", "address", "famsize", "Pstatus",
              "Mjob", "Fjob", "reason", "guardian", "schoolsup",
              "famsup", "higher", "internet", "romantic", "activities")

plot_list <- lapply(cat_vars[1:12], function(var) {
  ggplot(student, aes_string(x = var, y = "G3", fill = var)) +
    geom_boxplot(alpha = 0.75, outlier.size = 1, show.legend = FALSE) +
    stat_summary(fun = mean, geom = "point", shape = 18, size = 2,
                 color = "black") +
    labs(title = var, x = "", y = "G3") +
    theme_minimal(base_size = 10) +
    theme(plot.title = element_text(face = "bold", size = 10),
          axis.text.x = element_text(angle = 30, hjust = 1, size = 8))
})

do.call(grid.arrange, c(plot_list, ncol = 3,
                         top = "Distribución de G3 por variables categóricas"))
```

```{r eda-grades-evolution}
# Evolución de notas G1 → G2 → G3
grades_long <- student |>
  dplyr::select(G1, G2, G3, G3_bin) |>
  pivot_longer(cols = c(G1, G2, G3), names_to = "Periodo", values_to = "Nota")

ggplot(grades_long, aes(x = Periodo, y = Nota, fill = Periodo)) +
  geom_violin(alpha = 0.7) +
  geom_boxplot(width = 0.15, fill = "white", outlier.size = 0.8) +
  scale_fill_manual(values = c("#3498db", "#e67e22", "#2ecc71")) +
  labs(title = "Evolución de las Notas a lo Largo del Curso",
       subtitle = "G1 (1er periodo) → G2 (2º periodo) → G3 (final)",
       x = "Periodo", y = "Nota (0-20)", fill = "Periodo") +
  theme_minimal(base_size = 13) +
  theme(plot.title = element_text(face = "bold"), legend.position = "none")
```

```{r eda-scatter-matrix, fig.height=7}
# Scatterplot matrix de variables más correlacionadas con G3
key_vars <- c("G1", "G2", "studytime", "failures", "absences", "Medu", "G3")

GGally::ggpairs(
  student[, key_vars],
  upper = list(continuous = wrap("cor", size = 3.5, color = "#2c3e50")),
  lower = list(continuous = wrap("points", alpha = 0.3, size = 0.8,
                                  color = "#2c3e50")),
  diag  = list(continuous = wrap("densityDiag", fill = "#3498db", alpha = 0.5)),
  title = "Matriz de Dispersión — Variables Clave"
) +
  theme_minimal(base_size = 10)
```

::: callout-tip
**Hallazgos clave del EDA:**

-   **G1 y G2** tienen una correlación muy alta con G3 (r ≈ 0.87 y 0.90), lo que indica una gran consistencia en el rendimiento a lo largo del curso.
-   **failures** (número de suspensos previos) presenta correlación negativa importante (r ≈ -0.35).
-   **higher** (deseo de estudiar educación superior) está fuertemente asociado con mejores notas finales.
-   Variables como **studytime**, **Medu** y **Fedu** muestran correlaciones moderadas positivas.
-   El alcohol (**Dalc**, **Walc**) y **goout** presentan correlaciones negativas leves.
:::

------------------------------------------------------------------------

# Regresión Lineal Simple

## Selección del Predictor

Se elige **G2** (nota del segundo trimestre) como predictor simple por ser la variable más correlacionada con G3 (r ≈ 0.90) y por su evidente interpretación causal: el rendimiento acumulado predice el resultado final.

```{r slr-scatter}
ggplot(train_data, aes(x = G2, y = G3)) +
  geom_point(alpha = 0.4, color = "#2c3e50", size = 2) +
  geom_smooth(method = "lm", color = "#e74c3c", linewidth = 1.2, se = TRUE,
              fill = "#e74c3c", alpha = 0.15) +
  geom_smooth(method = "loess", color = "#3498db", linewidth = 1, linetype = "dashed",
              se = FALSE) +
  labs(title = "Relación G2 (2º Trimestre) vs G3 (Nota Final)",
       subtitle = "Rojo: regresión lineal | Azul: suavizado LOESS",
       x = "Nota 2º Trimestre (G2)", y = "Nota Final (G3)") +
  theme_minimal(base_size = 13) +
  theme(plot.title = element_text(face = "bold"))
```

## Ajuste del Modelo

```{r slr-fit}
# ── Modelo de Regresión Lineal Simple ─────────────────────────────────────
slr_model <- lm(G3 ~ G2, data = train_data)

cat("=== MODELO DE REGRESIÓN LINEAL SIMPLE: G3 ~ G2 ===\n\n")
summary(slr_model)
```

```{r slr-tidy}
# Tabla de coeficientes
tidy(slr_model, conf.int = TRUE) |>
  mutate(across(where(is.numeric), ~ round(.x, 4))) |>
  kable(caption = "Coeficientes del modelo de regresión lineal simple",
        col.names = c("Término", "Estimación", "Error Std.", "Estadístico t",
                      "p-valor", "IC 2.5%", "IC 97.5%")) |>
  kable_styling(bootstrap_options = c("striped", "hover"), full_width = FALSE) |>
  row_spec(2, background = "#d5f5e3", bold = TRUE)
```

::: callout-note
**Interpretación del modelo:**

-   **Intercepto (β₀ ≈ 0.97):** Valor esperado de G3 cuando G2 = 0, con poca interpretación práctica.
-   **Pendiente (β₁ ≈ 0.94):** Por cada punto adicional en G2, la nota final G3 aumenta en promedio **0.94 puntos**, manteniendo todo lo demás constante.
-   **R² ≈ 0.81:** El modelo explica aproximadamente el 81% de la variabilidad en G3, lo que indica un ajuste muy bueno para un modelo de una sola variable.
-   El coeficiente es altamente significativo (p \< 2e-16).
:::

## Diagnóstico de Residuos

```{r slr-residuals, fig.height=8}
# Gráficos de diagnóstico
par(mfrow = c(2, 2), mar = c(4, 4, 3, 1))
plot(slr_model,
     which = 1:4,
     col   = adjustcolor("#2c3e50", alpha.f = 0.5),
     pch   = 16,
     cex   = 0.8)
par(mfrow = c(1, 1))
```

```{r slr-residuals-analysis}
# Análisis formal de residuos
residuals_slr <- residuals(slr_model)
fitted_slr    <- fitted(slr_model)

# 1. Normalidad
sw_res <- shapiro.test(residuals_slr)
ad_res <- nortest::ad.test(residuals_slr)

# 2. Homocedasticidad
bp_test <- lmtest::bptest(slr_model)
gq_test <- lmtest::gqtest(slr_model)

# 3. Autocorrelación
dw_test <- lmtest::dwtest(slr_model)

cat("=== TESTS DE DIAGNÓSTICO DE RESIDUOS ===\n\n")
cat("1. NORMALIDAD DE RESIDUOS:\n")
cat(sprintf("   Shapiro-Wilk:      W = %.4f, p = %.4f\n",
            sw_res$statistic, sw_res$p.value))
cat(sprintf("   Anderson-Darling:  A = %.4f, p = %.4f\n",
            ad_res$statistic, ad_res$p.value))
cat("\n2. HOMOCEDASTICIDAD:\n")
cat(sprintf("   Breusch-Pagan:     BP = %.4f, p = %.4f\n",
            bp_test$statistic, bp_test$p.value))
cat(sprintf("   Goldfeld-Quandt:   F  = %.4f, p = %.4f\n",
            gq_test$statistic, gq_test$p.value))
cat("\n3. AUTOCORRELACIÓN (Durbin-Watson):\n")
cat(sprintf("   DW = %.4f, p = %.4f\n",
            dw_test$statistic, dw_test$p.value))
```

```{r slr-residuals-plots}
res_df <- data.frame(
  Fitted    = fitted_slr,
  Residuals = residuals_slr,
  StdRes    = rstandard(slr_model)
)

p_res1 <- ggplot(res_df, aes(x = Fitted, y = Residuals)) +
  geom_point(alpha = 0.5, color = "#2c3e50") +
  geom_hline(yintercept = 0, color = "#e74c3c", linewidth = 1) +
  geom_smooth(se = FALSE, color = "#3498db", linewidth = 0.8) +
  labs(title = "Residuos vs Valores Ajustados",
       x = "Valores ajustados", y = "Residuos") +
  theme_minimal(base_size = 12)

p_res2 <- ggplot(res_df, aes(x = Residuals)) +
  geom_histogram(aes(y = after_stat(density)), bins = 25,
                 fill = "#3498db", alpha = 0.7, color = "white") +
  geom_density(color = "#e74c3c", linewidth = 1.2) +
  stat_function(fun = dnorm,
                args = list(mean = mean(residuals_slr), sd = sd(residuals_slr)),
                color = "#2ecc71", linewidth = 1, linetype = "dashed") +
  labs(title = "Distribución de Residuos",
       subtitle = "Azul: densidad empírica | Verde: normal teórica",
       x = "Residuos", y = "Densidad") +
  theme_minimal(base_size = 12)

p_res3 <- ggplot(res_df, aes(sample = StdRes)) +
  stat_qq(alpha = 0.6, color = "#2c3e50") +
  stat_qq_line(color = "#e74c3c", linewidth = 1) +
  labs(title = "Q-Q Plot de Residuos Estandarizados",
       x = "Cuantiles teóricos", y = "Cuantiles observados") +
  theme_minimal(base_size = 12)

p_res4 <- ggplot(res_df, aes(x = Fitted, y = sqrt(abs(StdRes)))) +
  geom_point(alpha = 0.5, color = "#2c3e50") +
  geom_smooth(se = FALSE, color = "#e74c3c", linewidth = 0.8) +
  labs(title = "Scale-Location",
       x = "Valores ajustados", y = "√|Residuos estandarizados|") +
  theme_minimal(base_size = 12)

grid.arrange(p_res1, p_res2, p_res3, p_res4, ncol = 2,
             top = "Diagnóstico Completo de Residuos — Regresión Lineal Simple")
```

## Análisis de Puntos Influyentes

```{r slr-influence}
# Estadísticos de influencia
inf_stats <- influence.measures(slr_model)
cook_d    <- cooks.distance(slr_model)
leverage  <- hatvalues(slr_model)

# Umbrales
n_train  <- nrow(train_data)
p_slr    <- 2  # intercepto + 1 predictor
lev_threshold  <- 2 * p_slr / n_train
cook_threshold <- 4 / n_train

influence_df <- data.frame(
  Obs      = 1:n_train,
  CookD    = cook_d,
  Leverage = leverage,
  StdRes   = rstandard(slr_model)
) |>
  mutate(
    Influential = CookD > cook_threshold,
    HighLev     = Leverage > lev_threshold,
    Outlier     = abs(StdRes) > 3
  )

cat(sprintf("Puntos con Cook's D > %.4f: %d\n", cook_threshold,
            sum(influence_df$Influential)))
cat(sprintf("Puntos con leverage > %.4f: %d\n", lev_threshold,
            sum(influence_df$HighLev)))
cat(sprintf("Outliers (|residuo std| > 3): %d\n", sum(influence_df$Outlier)))

ggplot(influence_df, aes(x = Leverage, y = CookD, color = Influential)) +
  geom_point(aes(size = abs(StdRes)), alpha = 0.7) +
  geom_hline(yintercept = cook_threshold, linetype = "dashed",
             color = "#e74c3c", linewidth = 0.8) +
  geom_vline(xintercept = lev_threshold, linetype = "dashed",
             color = "#3498db", linewidth = 0.8) +
  scale_color_manual(values = c("FALSE" = "#2c3e50", "TRUE" = "#e74c3c")) +
  labs(title = "Análisis de Puntos Influyentes",
       subtitle = "Rojo: Cook's D alto | Azul: leverage alto",
       x = "Leverage (hat values)", y = "Distancia de Cook",
       color = "Influyente", size = "|Residuo std|") +
  theme_minimal(base_size = 12)
```

## Transformación de Variables

```{r slr-transformation}
# Análisis de si la relación es lineal o requiere transformación
# Comparamos modelo lineal vs modelos transformados

# Modelo con G2 al cuadrado (componente cuadrático)
slr_quad <- lm(G3 ~ G2 + I(G2^2), data = train_data)

# Modelo con log(G2+1)
slr_log  <- lm(G3 ~ log(G2 + 1), data = train_data)

# Modelo con sqrt(G2)
slr_sqrt <- lm(G3 ~ sqrt(G2), data = train_data)

# Comparación
compare_slr <- data.frame(
  Modelo   = c("Lineal: G3 ~ G2",
               "Cuadrático: G3 ~ G2 + G2²",
               "Logarítmico: G3 ~ log(G2+1)",
               "Raíz cuadrada: G3 ~ sqrt(G2)"),
  R2       = c(summary(slr_model)$r.squared,
               summary(slr_quad)$r.squared,
               summary(slr_log)$r.squared,
               summary(slr_sqrt)$r.squared),
  R2_adj   = c(summary(slr_model)$adj.r.squared,
               summary(slr_quad)$adj.r.squared,
               summary(slr_log)$adj.r.squared,
               summary(slr_sqrt)$adj.r.squared),
  AIC      = c(AIC(slr_model), AIC(slr_quad),
               AIC(slr_log),   AIC(slr_sqrt)),
  BIC      = c(BIC(slr_model), BIC(slr_quad),
               BIC(slr_log),   BIC(slr_sqrt)),
  RMSE_val = sapply(list(slr_model, slr_quad, slr_log, slr_sqrt), function(m) {
    # Para modelos con transformación en X, predecimos normalmente
    pred <- predict(m, newdata = val_data)
    sqrt(mean((val_data$G3 - pred)^2))
  })
) |>
  mutate(across(where(is.numeric), ~ round(.x, 4)))

compare_slr |>
  kable(caption = "Comparación de transformaciones — Regresión Lineal Simple") |>
  kable_styling(bootstrap_options = c("striped", "hover"), full_width = FALSE) |>
  row_spec(which.min(compare_slr$AIC), background = "#d5f5e3", bold = TRUE)
```

```{r slr-transform-plot}
# Visualización de las transformaciones
g2_seq <- seq(0, 20, by = 0.1)
pred_df <- data.frame(
  G2 = g2_seq,
  Lineal      = predict(slr_model, newdata = data.frame(G2 = g2_seq)),
  Cuadratico  = predict(slr_quad,  newdata = data.frame(G2 = g2_seq)),
  Logaritmico = predict(slr_log,   newdata = data.frame(G2 = g2_seq)),
  RaizCuad    = predict(slr_sqrt,  newdata = data.frame(G2 = g2_seq))
) |>
  pivot_longer(-G2, names_to = "Modelo", values_to = "Pred")

ggplot() +
  geom_point(data = train_data, aes(x = G2, y = G3),
             alpha = 0.3, color = "gray50", size = 1.5) +
  geom_line(data = pred_df, aes(x = G2, y = Pred, color = Modelo), linewidth = 1) +
  scale_color_brewer(palette = "Set1") +
  labs(title = "Comparación de Transformaciones en Regresión Simple",
       x = "G2", y = "G3", color = "Modelo") +
  theme_minimal(base_size = 13) +
  theme(plot.title = element_text(face = "bold"))
```

## Evaluación en Validación y Test

```{r slr-evaluation}
# Evaluación del modelo lineal simple (mejor según AIC)
pred_val_slr  <- predict(slr_model, newdata = val_data)
pred_test_slr <- predict(slr_model, newdata = test_data)

metrics_slr <- data.frame(
  Conjunto = c("Validación", "Test"),
  RMSE     = c(sqrt(mean((val_data$G3  - pred_val_slr)^2)),
               sqrt(mean((test_data$G3 - pred_test_slr)^2))),
  MAE      = c(mean(abs(val_data$G3  - pred_val_slr)),
               mean(abs(test_data$G3 - pred_test_slr))),
  R2       = c(cor(val_data$G3,  pred_val_slr)^2,
               cor(test_data$G3, pred_test_slr)^2)
) |>
  mutate(across(where(is.numeric), ~ round(.x, 4)))

metrics_slr |>
  kable(caption = "Métricas del modelo de regresión lineal simple") |>
  kable_styling(bootstrap_options = c("striped", "hover"), full_width = FALSE)

# Gráfico predicciones vs reales (test)
ggplot(data.frame(Real = test_data$G3, Pred = pred_test_slr),
       aes(x = Real, y = Pred)) +
  geom_point(alpha = 0.6, color = "#2c3e50", size = 2) +
  geom_abline(slope = 1, intercept = 0, color = "#e74c3c",
              linewidth = 1.2, linetype = "dashed") +
  geom_smooth(method = "lm", color = "#3498db", se = FALSE) +
  labs(title = "Predicciones vs Valores Reales — Test (Regresión Simple)",
       subtitle = "Línea roja: predicción perfecta",
       x = "Valores reales (G3)", y = "Valores predichos") +
  theme_minimal(base_size = 13) +
  theme(plot.title = element_text(face = "bold"))
```

::: callout-tip
**Conclusión del modelo simple:** El modelo G3 \~ G2 es excepcionalmente bueno para ser univariante (R² ≈ 0.81 en test). La relación es prácticamente lineal y no requiere transformaciones que mejoren significativamente el ajuste. El modelo cuadrático mejora marginalmente el AIC pero añade complejidad innecesaria. **Se mantiene el modelo lineal simple como referencia.**
:::

------------------------------------------------------------------------

# Regresión Lineal Múltiple

## Preparación de Variables para Modelado

```{r mlr-prep}
# Variables predictoras (excluimos G3_bin, G_avg, y variables derivadas)
# También excluimos G1 y G2 en algunos modelos para ver el poder predictivo
# de las variables sociodemográficas puras

# Definimos dos conjuntos de fórmulas:
# A) Con G1 y G2 (máximo rendimiento predictivo)
# B) Sin G1 y G2 (interpretación sociodemográfica pura)

# Preparamos datos de entrenamiento con todas las variables relevantes
train_mlr <- train_data |>
  dplyr::select(-G3_bin, -G_avg, -Alc_total, -ParentEdu,
         -studytime_f, -traveltime_f)

val_mlr  <- val_data  |> dplyr::select(names(train_mlr))
test_mlr <- test_data |> dplyr::select(names(train_mlr))

cat("Variables disponibles para el modelo múltiple:\n")
cat(paste(names(train_mlr), collapse = ", "), "\n")
cat("\nTotal de variables:", ncol(train_mlr) - 1, "(excl. G3)\n")
```

## Modelo de Regresión Lineal Múltiple Completo

```{r mlr-full}
# Modelo completo con todas las variables (incluye G1, G2)
mlr_full <- lm(G3 ~ ., data = train_mlr)

cat("=== MODELO COMPLETO ===\n")
cat(sprintf("R²:          %.4f\n", summary(mlr_full)$r.squared))
cat(sprintf("R² ajustado: %.4f\n", summary(mlr_full)$adj.r.squared))
cat(sprintf("AIC:         %.2f\n", AIC(mlr_full)))
cat(sprintf("BIC:         %.2f\n", BIC(mlr_full)))
cat(sprintf("F-stat:      %.2f (p = %.2e)\n",
            summary(mlr_full)$fstatistic[1],
            pf(summary(mlr_full)$fstatistic[1],
               summary(mlr_full)$fstatistic[2],
               summary(mlr_full)$fstatistic[3], lower.tail = FALSE)))
```

## Análisis de Multicolinealidad (VIF)

```{r mlr-vif}
# Factor de Inflación de la Varianza
vif_values <- car::vif(mlr_full)

# Para factores, GVIF^(1/(2*Df)) es la medida comparable
if (is.matrix(vif_values)) {
  vif_df <- data.frame(
    Variable = rownames(vif_values),
    GVIF     = round(vif_values[, 1], 3),
    Df       = vif_values[, 2],
    GVIF_adj = round(vif_values[, 3], 3)
  ) |>
    mutate(Problema = case_when(
      GVIF_adj > 10 ~ "⚠️ Severo",
      GVIF_adj > 5  ~ "⚠️ Moderado",
      GVIF_adj > 2  ~ "Leve",
      TRUE          ~ "✓ Aceptable"
    )) |>
    arrange(desc(GVIF_adj))
} else {
  vif_df <- data.frame(
    Variable = names(vif_values),
    VIF      = round(vif_values, 3)
  ) |>
    mutate(Problema = case_when(
      VIF > 10 ~ "⚠️ Severo",
      VIF > 5  ~ "⚠️ Moderado",
      VIF > 2  ~ "Leve",
      TRUE     ~ "✓ Aceptable"
    )) |>
    arrange(desc(VIF))
}

vif_df |>
  kable(caption = "Factor de Inflación de la Varianza (VIF/GVIF)") |>
  kable_styling(bootstrap_options = c("striped", "hover"), full_width = FALSE) |>
  row_spec(which(grepl("Severo", vif_df$Problema)),
           background = "#fadbd8", bold = TRUE) |>
  row_spec(which(grepl("Moderado", vif_df$Problema)),
           background = "#fef9e7")
```

## Selección de Variables

### Método 1: Selección por Subconjuntos (Best Subset)

```{r mlr-subset, fig.height=6}
# Best subset selection — limitado a variables numéricas para eficiencia
num_predictors <- c("age", "Medu", "Fedu", "traveltime", "studytime",
                    "failures", "famrel", "freetime", "goout",
                    "Dalc", "Walc", "health", "absences", "G1", "G2")

train_num <- train_data[, c(num_predictors, "G3")]

regfit <- leaps::regsubsets(G3 ~ ., data = train_num,
                             nvmax = length(num_predictors),
                             method = "exhaustive")
reg_summary <- summary(regfit)

# Gráfico de criterios de selección
par(mfrow = c(2, 2), mar = c(4, 4, 3, 1))

plot(reg_summary$rss,    xlab = "Nº variables", ylab = "RSS",
     type = "b", pch = 16, col = "#2c3e50")
title("RSS")

plot(reg_summary$adjr2,  xlab = "Nº variables", ylab = "R² ajustado",
     type = "b", pch = 16, col = "#2c3e50")
points(which.max(reg_summary$adjr2), max(reg_summary$adjr2),
       col = "#e74c3c", cex = 2, pch = 16)
title("R² Ajustado")

plot(reg_summary$cp,     xlab = "Nº variables", ylab = "Cp de Mallows",
     type = "b", pch = 16, col = "#2c3e50")
abline(0, 1, col = "#e74c3c", lty = 2)
title("Cp de Mallows")

plot(reg_summary$bic,    xlab = "Nº variables", ylab = "BIC",
     type = "b", pch = 16, col = "#2c3e50")
points(which.min(reg_summary$bic), min(reg_summary$bic),
       col = "#e74c3c", cex = 2, pch = 16)
title("BIC")

par(mfrow = c(1, 1))
```

```{r mlr-subset-best}
best_r2  <- which.max(reg_summary$adjr2)
best_bic <- which.min(reg_summary$bic)
best_cp  <- which.min(abs(reg_summary$cp - (1:length(reg_summary$cp))))

cat(sprintf("Mejor modelo por R² ajustado: %d variables\n", best_r2))
cat(sprintf("Mejor modelo por BIC:          %d variables\n", best_bic))
cat(sprintf("Mejor modelo por Cp:           %d variables\n", best_cp))

# Variables del mejor modelo BIC
coef_best_bic <- coef(regfit, best_bic)
cat(sprintf("\nVariables en el mejor modelo BIC (%d vars):\n", best_bic))
print(names(coef_best_bic)[-1])
```

### Método 2: Selección Stepwise (AIC y BIC)

```{r mlr-stepwise}
# Stepwise AIC
mlr_step_aic <- step(mlr_full, direction = "both",
                     trace = 0, k = 2)

# Stepwise BIC
mlr_step_bic <- step(mlr_full, direction = "both",
                     trace = 0, k = log(nrow(train_mlr)))

cat("=== STEPWISE AIC ===\n")
cat("Variables seleccionadas:\n")
cat(paste(names(coef(mlr_step_aic))[-1], collapse = ", "), "\n")
cat(sprintf("AIC: %.2f | R²adj: %.4f\n",
            AIC(mlr_step_aic), summary(mlr_step_aic)$adj.r.squared))

cat("\n=== STEPWISE BIC ===\n")
cat("Variables seleccionadas:\n")
cat(paste(names(coef(mlr_step_bic))[-1], collapse = ", "), "\n")
cat(sprintf("BIC: %.2f | R²adj: %.4f\n",
            BIC(mlr_step_bic), summary(mlr_step_bic)$adj.r.squared))
```

### Método 3: Regularización — Ridge, Lasso y Elastic Net

```{r mlr-regularization}
# ── Preparación de matrices para glmnet ───────────────────────────────────
# Variables para regularización (con G1 y G2)
reg_vars <- c("age", "Medu", "Fedu", "traveltime", "studytime", "failures",
              "famrel", "freetime", "goout", "Dalc", "Walc", "health",
              "absences", "G1", "G2",
              "school", "sex", "address", "famsize", "Pstatus",
              "schoolsup", "famsup", "paid", "activities", "nursery",
              "higher", "internet", "romantic",
              "Mjob", "Fjob", "reason", "guardian")

# Crear matrices de diseño (one-hot encoding automático)
X_train <- model.matrix(G3 ~ ., data = train_data[, c(reg_vars, "G3")])[, -1]
X_val   <- model.matrix(G3 ~ ., data = val_data[,   c(reg_vars, "G3")])[, -1]
X_test  <- model.matrix(G3 ~ ., data = test_data[,  c(reg_vars, "G3")])[, -1]

y_train <- train_data$G3
y_val   <- val_data$G3
y_test  <- test_data$G3

# ── Ridge (α = 0) ─────────────────────────────────────────────────────────
cv_ridge <- cv.glmnet(X_train, y_train, alpha = 0, nfolds = 10)
ridge_best <- glmnet(X_train, y_train, alpha = 0, lambda = cv_ridge$lambda.min)

# ── Lasso (α = 1) ─────────────────────────────────────────────────────────
cv_lasso <- cv.glmnet(X_train, y_train, alpha = 1, nfolds = 10)
lasso_best <- glmnet(X_train, y_train, alpha = 1, lambda = cv_lasso$lambda.min)

# ── Elastic Net (α = 0.5) ──────────────────────────────────────────────────
cv_enet  <- cv.glmnet(X_train, y_train, alpha = 0.5, nfolds = 10)
enet_best <- glmnet(X_train, y_train, alpha = 0.5, lambda = cv_enet$lambda.min)

cat("=== REGULARIZACIÓN — λ ÓPTIMOS (CV 10-fold) ===\n")
cat(sprintf("Ridge:        λ.min = %.5f | λ.1se = %.5f\n",
            cv_ridge$lambda.min, cv_ridge$lambda.1se))
cat(sprintf("Lasso:        λ.min = %.5f | λ.1se = %.5f\n",
            cv_lasso$lambda.min, cv_lasso$lambda.1se))
cat(sprintf("Elastic Net:  λ.min = %.5f | λ.1se = %.5f\n",
            cv_enet$lambda.min,  cv_enet$lambda.1se))
```

```{r mlr-reg-plots, fig.height=5}
par(mfrow = c(1, 3))
plot(cv_ridge, main = "CV Ridge (α=0)")
plot(cv_lasso, main = "CV Lasso (α=1)")
plot(cv_enet,  main = "CV Elastic Net (α=0.5)")
par(mfrow = c(1, 1))
```

```{r mlr-lasso-coefs}
# Coeficientes Lasso — variables seleccionadas (no nulas)
lasso_coefs <- coef(lasso_best)
lasso_df <- data.frame(
  Variable   = rownames(lasso_coefs),
  Coeficiente = as.numeric(lasso_coefs)
) |>
  filter(Coeficiente != 0, Variable != "(Intercept)") |>
  arrange(desc(abs(Coeficiente)))

cat(sprintf("Variables seleccionadas por Lasso: %d de %d\n",
            nrow(lasso_df), ncol(X_train)))

lasso_df |>
  mutate(Coeficiente = round(Coeficiente, 4)) |>
  kable(caption = "Coeficientes no nulos del modelo Lasso") |>
  kable_styling(bootstrap_options = c("striped", "hover"), full_width = FALSE) |>
  row_spec(which(lasso_df$Coeficiente > 0), background = "#d5f5e3") |>
  row_spec(which(lasso_df$Coeficiente < 0), background = "#fadbd8")
```

```{r mlr-regularization-paths, fig.height=5}
# Trayectorias de regularización Ridge y Lasso
par(mfrow = c(1, 2))
ridge_path <- glmnet(X_train, y_train, alpha = 0)
lasso_path <- glmnet(X_train, y_train, alpha = 1)

plot(ridge_path, xvar = "lambda", label = FALSE,
     main = "Trayectorias de coeficientes — Ridge")
abline(v = log(cv_ridge$lambda.min), col = "#e74c3c", lty = 2, lwd = 1.5)

plot(lasso_path, xvar = "lambda", label = FALSE,
     main = "Trayectorias de coeficientes — Lasso")
abline(v = log(cv_lasso$lambda.min), col = "#e74c3c", lty = 2, lwd = 1.5)
par(mfrow = c(1, 1))
```

## Modelos No Lineales y Transformación de Variables

### Exploración de No Linealidades

```{r mlr-nonlinear-exploration}
# Componentes parciales (Component + Residual plots)
# Modelo base con variables clave para análisis no lineal
mlr_base <- lm(G3 ~ G2 + G1 + failures + absences + studytime +
                 Medu + Fedu + goout + Walc, data = train_data)

car::crPlots(mlr_base,
             terms  = ~ G2 + G1 + failures + absences + studytime,
             smooth = list(smoother = car::loessLine),
             col    = adjustcolor("#2c3e50", 0.5),
             pch    = 16,
             cex    = 0.7,
             main   = "Component + Residual Plots (Variables Clave)")
```

```{r mlr-nonlinear-models}
# Modelos con términos no lineales basados en la exploración visual
# 1. Modelo con splines/polinomios para variables no lineales
mlr_poly <- lm(G3 ~ G2 + G1 +
                 poly(failures, 2) +
                 poly(absences, 2) +
                 studytime + Medu + Fedu +
                 goout + Walc + higher + schoolsup,
               data = train_data)

# 2. Modelo con interacciones relevantes
mlr_inter <- lm(G3 ~ G2 + G1 +
                  failures * schoolsup +
                  studytime * higher +
                  Medu + Fedu +
                  absences + goout + Walc,
                data = train_data)

# 3. Modelo con log-transformación de absences y failures
train_nl <- train_data |>
  mutate(log_abs = log(absences + 1),
         fail_cat = factor(ifelse(failures == 0, "ninguno",
                                  ifelse(failures == 1, "uno", "dos_o_mas"))))
val_nl  <- val_data  |>
  mutate(log_abs = log(absences + 1),
         fail_cat = factor(ifelse(failures == 0, "ninguno",
                                  ifelse(failures == 1, "uno", "dos_o_mas")),
                           levels = levels(train_nl$fail_cat)))
test_nl <- test_data |>
  mutate(log_abs = log(absences + 1),
         fail_cat = factor(ifelse(failures == 0, "ninguno",
                                  ifelse(failures == 1, "uno", "dos_o_mas")),
                           levels = levels(train_nl$fail_cat)))

mlr_log_trans <- lm(G3 ~ G2 + G1 + fail_cat + log_abs +
                      studytime + Medu + Fedu + goout + Walc +
                      higher + schoolsup,
                    data = train_nl)

compare_nonlinear <- data.frame(
  Modelo = c("Base (lineal)", "Polinómico (failures², absences²)",
             "Con interacciones", "Log-transformado (absences)"),
  R2     = round(c(summary(mlr_base)$r.squared,
                   summary(mlr_poly)$r.squared,
                   summary(mlr_inter)$r.squared,
                   summary(mlr_log_trans)$r.squared), 4),
  R2adj  = round(c(summary(mlr_base)$adj.r.squared,
                   summary(mlr_poly)$adj.r.squared,
                   summary(mlr_inter)$adj.r.squared,
                   summary(mlr_log_trans)$adj.r.squared), 4),
  AIC    = round(c(AIC(mlr_base), AIC(mlr_poly),
                   AIC(mlr_inter), AIC(mlr_log_trans)), 2),
  BIC    = round(c(BIC(mlr_base), BIC(mlr_poly),
                   BIC(mlr_inter), BIC(mlr_log_trans)), 2),
  RMSE_val = round(c(
    sqrt(mean((val_data$G3  - predict(mlr_base,      val_data))^2)),
    sqrt(mean((val_data$G3  - predict(mlr_poly,      val_data))^2)),
    sqrt(mean((val_data$G3  - predict(mlr_inter,     val_data))^2)),
    sqrt(mean((val_nl$G3    - predict(mlr_log_trans, val_nl))^2))
  ), 4)
)

compare_nonlinear |>
  kable(caption = "Comparación de modelos no lineales") |>
  kable_styling(bootstrap_options = c("striped", "hover"), full_width = FALSE) |>
  row_spec(which.min(compare_nonlinear$AIC), background = "#d5f5e3", bold = TRUE)
```

## Selección del Mejor Modelo Múltiple

```{r mlr-model-selection}
# Evaluación de todos los candidatos en validación
models_list <- list(
  "MLR Completo"         = list(model = mlr_full,      data_val = val_mlr,
                                 data_test = test_mlr),
  "Stepwise AIC"         = list(model = mlr_step_aic,  data_val = val_data,
                                 data_test = test_data),
  "Stepwise BIC"         = list(model = mlr_step_bic,  data_val = val_data,
                                 data_test = test_data),
  "Polinómico"           = list(model = mlr_poly,      data_val = val_data,
                                 data_test = test_data),
  "Con Interacciones"    = list(model = mlr_inter,     data_val = val_data,
                                 data_test = test_data),
  "Log-Transformado"     = list(model = mlr_log_trans, data_val = val_nl,
                                 data_test = test_nl)
)

eval_models <- function(model, data_val, data_test) {
  pred_val  <- predict(model, newdata = data_val)
  pred_test <- predict(model, newdata = data_test)
  data.frame(
    RMSE_val  = sqrt(mean((data_val$G3  - pred_val)^2)),
    MAE_val   = mean(abs(data_val$G3    - pred_val)),
    R2_val    = cor(data_val$G3, pred_val)^2,
    RMSE_test = sqrt(mean((data_test$G3 - pred_test)^2)),
    MAE_test  = mean(abs(data_test$G3   - pred_test)),
    R2_test   = cor(data_test$G3, pred_test)^2,
    AIC       = AIC(model),
    BIC       = BIC(model),
    R2_adj    = summary(model)$adj.r.squared
  )
}

# Evaluación Ridge, Lasso, Elastic Net
eval_reg <- function(model, X_val, X_test) {
  pred_val  <- predict(model, newx = X_val)
  pred_test <- predict(model, newx = X_test)
  data.frame(
    RMSE_val  = sqrt(mean((y_val  - pred_val)^2)),
    MAE_val   = mean(abs(y_val    - pred_val)),
    R2_val    = cor(y_val, pred_val)^2,
    RMSE_test = sqrt(mean((y_test - pred_test)^2)),
    MAE_test  = mean(abs(y_test   - pred_test)),
    R2_test   = cor(y_test, pred_test)^2,
    AIC       = NA,
    BIC       = NA,
    R2_adj    = NA
  )
}

results_df <- bind_rows(
  lapply(names(models_list), function(nm) {
    m  <- models_list[[nm]]
    ev <- eval_models(m$model, m$data_val, m$data_test)
    ev$Modelo <- nm
    ev
  }),
  {ev <- eval_reg(ridge_best, X_val, X_test); ev$Modelo <- "Ridge"; ev},
  {ev <- eval_reg(lasso_best, X_val, X_test); ev$Modelo <- "Lasso"; ev},
  {ev <- eval_reg(enet_best,  X_val, X_test); ev$Modelo <- "Elastic Net"; ev}
) |>
  dplyr::select(Modelo, everything()) |>
  mutate(across(where(is.numeric), ~ round(.x, 4)))

results_df |>
  kable(caption = "Comparación completa de todos los modelos de regresión múltiple") |>
  kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
                font_size = 12, full_width = TRUE) |>
  row_spec(which.min(results_df$RMSE_val), background = "#d5f5e3", bold = TRUE)
```

```{r mlr-best-model}
# El modelo Stepwise AIC/BIC suele ser el mejor balance — confirmamos con validación
best_mlr_name <- results_df$Modelo[which.min(results_df$RMSE_val)]
cat("Mejor modelo (menor RMSE en validación):", best_mlr_name, "\n")

# Seleccionamos el Stepwise AIC como mejor modelo múltiple interpretable
best_mlr <- mlr_step_aic
cat("\n=== RESUMEN DEL MEJOR MODELO MÚLTIPLE ===\n")
summary(best_mlr)
```

## Diagnóstico del Mejor Modelo Múltiple

```{r mlr-diagnostics, fig.height=8}
# Gráficos de diagnóstico del mejor modelo
par(mfrow = c(2, 3), mar = c(4, 4, 3, 1))
plot(best_mlr, which = 1:6, col = adjustcolor("#2c3e50", 0.5),
     pch = 16, cex = 0.7)
par(mfrow = c(1, 1))
```

```{r mlr-diagnostics-formal}
# Tests formales
residuals_mlr <- residuals(best_mlr)

cat("=== DIAGNÓSTICO FORMAL DEL MEJOR MODELO MÚLTIPLE ===\n\n")

# 1. Normalidad
sw_mlr <- shapiro.test(residuals_mlr)
cat(sprintf("1. Normalidad (Shapiro-Wilk):   W = %.4f, p = %.4f → %s\n",
            sw_mlr$statistic, sw_mlr$p.value,
            ifelse(sw_mlr$p.value > 0.05, "✓ No se rechaza normalidad",
                   "⚠ Se rechaza normalidad")))

# 2. Homocedasticidad
bp_mlr <- lmtest::bptest(best_mlr)
cat(sprintf("2. Homocedasticidad (BP):       BP = %.4f, p = %.4f → %s\n",
            bp_mlr$statistic, bp_mlr$p.value,
            ifelse(bp_mlr$p.value > 0.05, "✓ Homocedasticidad",
                   "⚠ Heterocedasticidad")))

# 3. Autocorrelación
dw_mlr <- lmtest::dwtest(best_mlr)
cat(sprintf("3. Autocorrelación (DW):        DW = %.4f, p = %.4f → %s\n",
            dw_mlr$statistic, dw_mlr$p.value,
            ifelse(dw_mlr$p.value > 0.05, "✓ Sin autocorrelación",
                   "⚠ Posible autocorrelación")))

# 4. VIF del mejor modelo
cat("\n4. VIF del mejor modelo:\n")
vif_best <- car::vif(best_mlr)
if (is.matrix(vif_best)) {
  print(round(vif_best[, 3], 3))
} else {
  print(round(vif_best, 3))
}
```

```{r mlr-diagnostics-visual, fig.height=5}
# Visualización avanzada de residuos
res_df_mlr <- data.frame(
  Fitted    = fitted(best_mlr),
  Residuals = residuals(best_mlr),
  StdRes    = rstandard(best_mlr),
  CooksD    = cooks.distance(best_mlr)
)

p1 <- ggplot(res_df_mlr, aes(x = Fitted, y = Residuals)) +
  geom_point(alpha = 0.5, color = "#2c3e50") +
  geom_hline(yintercept = 0, color = "#e74c3c", linewidth = 1) +
  geom_smooth(se = FALSE, color = "#3498db") +
  labs(title = "Residuos vs Ajustados", x = "Valores ajustados", y = "Residuos") +
  theme_minimal(base_size = 12)

p2 <- ggplot(res_df_mlr, aes(x = StdRes)) +
  geom_histogram(aes(y = after_stat(density)), bins = 25,
                 fill = "#3498db", alpha = 0.7, color = "white") +
  geom_density(color = "#e74c3c", linewidth = 1.2) +
  stat_function(fun = dnorm,
                args = list(mean = mean(res_df_mlr$StdRes),
                            sd = sd(res_df_mlr$StdRes)),
                color = "#2ecc71", linetype = "dashed", linewidth = 1) +
  labs(title = "Distribución de Residuos Estand.",
       x = "Residuos Estand.", y = "Densidad") +
  theme_minimal(base_size = 12)

p3 <- ggplot(res_df_mlr, aes(x = seq_along(CooksD), y = CooksD)) +
  geom_col(aes(fill = CooksD > 4/nrow(train_data)), alpha = 0.8) +
  scale_fill_manual(values = c("FALSE" = "#2c3e50", "TRUE" = "#e74c3c")) +
  geom_hline(yintercept = 4/nrow(train_data), linetype = "dashed",
             color = "#e74c3c") +
  labs(title = "Distancias de Cook",
       x = "Observación", y = "Cook's D") +
  theme_minimal(base_size = 12) +
  theme(legend.position = "none")

grid.arrange(p1, p2, p3, ncol = 3)
```

## Interpretación del Mejor Modelo

```{r mlr-interpretation}
# Coeficientes estandarizados para comparar importancia
# Estandarizamos manualmente las variables numéricas
coef_df <- tidy(best_mlr, conf.int = TRUE) |>
  filter(term != "(Intercept)") |>
  arrange(desc(abs(estimate))) |>
  mutate(across(where(is.numeric), ~ round(.x, 4)),
         Significativo = case_when(
           p.value < 0.001 ~ "***",
           p.value < 0.01  ~ "**",
           p.value < 0.05  ~ "*",
           p.value < 0.1   ~ ".",
           TRUE            ~ ""
         ))

coef_df |>
  dplyr::select(term, estimate, std.error, statistic, p.value,
         conf.low, conf.high, Significativo) |>
  kable(caption = "Coeficientes del mejor modelo de regresión múltiple",
        col.names = c("Variable", "Estimación", "Error Std.", "t", "p-valor",
                      "IC 2.5%", "IC 97.5%", "Sig.")) |>
  kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
                font_size = 12, full_width = FALSE) |>
  row_spec(which(coef_df$p.value < 0.05), background = "#d5f5e3") |>
  scroll_box(height = "400px")
```

```{r mlr-coef-plot}
# Gráfico de coeficientes con IC
coef_plot_df <- coef_df |>
  filter(p.value < 0.1) |>
  mutate(term = reorder(term, estimate))

ggplot(coef_plot_df, aes(x = estimate, y = term, color = estimate > 0)) +
  geom_vline(xintercept = 0, linetype = "dashed", color = "gray50") +
  geom_errorbarh(aes(xmin = conf.low, xmax = conf.high), height = 0.3,
                 linewidth = 0.8, alpha = 0.7) +
  geom_point(size = 3) +
  scale_color_manual(values = c("TRUE" = "#2ecc71", "FALSE" = "#e74c3c"),
                     labels = c("Negativo", "Positivo"), name = "Dirección") +
  labs(title = "Coeficientes Significativos del Mejor Modelo Múltiple",
       subtitle = "Variables con p < 0.1 | IC 95%",
       x = "Coeficiente estimado", y = "") +
  theme_minimal(base_size = 12) +
  theme(plot.title = element_text(face = "bold"))
```

------------------------------------------------------------------------

# Regresión Logística (Target Binarizado)

## Preparación y Descripción del Target Binario

```{r logistic-prep}
cat("=== TARGET BINARIO: G3_bin ===\n")
cat("Aprobado:   G3 ≥ 10\n")
cat("Suspendido: G3 <  10\n\n")

cat("Distribución en cada conjunto:\n")
cat(sprintf("  Train: %d aprobados (%.1f%%), %d suspendidos (%.1f%%)\n",
            sum(train_data$G3_bin == "Aprobado"),
            mean(train_data$G3_bin == "Aprobado") * 100,
            sum(train_data$G3_bin == "Suspendido"),
            mean(train_data$G3_bin == "Suspendido") * 100))
cat(sprintf("  Val:   %d aprobados (%.1f%%), %d suspendidos (%.1f%%)\n",
            sum(val_data$G3_bin == "Aprobado"),
            mean(val_data$G3_bin == "Aprobado") * 100,
            sum(val_data$G3_bin == "Suspendido"),
            mean(val_data$G3_bin == "Suspendido") * 100))
cat(sprintf("  Test:  %d aprobados (%.1f%%), %d suspendidos (%.1f%%)\n",
            sum(test_data$G3_bin == "Aprobado"),
            mean(test_data$G3_bin == "Aprobado") * 100,
            sum(test_data$G3_bin == "Suspendido"),
            mean(test_data$G3_bin == "Suspendido") * 100))
```

## Modelo Logístico Completo

```{r logistic-full}
# Modelo logístico completo (sin G1, G2 para análisis sociodemográfico puro,
# y con ellos para máxima predicción)

# Con G1 y G2
logit_full <- glm(G3_bin ~ . - G3 - G_avg - Alc_total - ParentEdu
                          - studytime_f - traveltime_f,
                  data   = train_data,
                  family = binomial(link = "logit"))

cat("=== MODELO LOGÍSTICO COMPLETO ===\n")
cat(sprintf("AIC: %.2f | Deviance residual: %.4f\n",
            AIC(logit_full), logit_full$deviance))
cat(sprintf("Pseudo R² (McFadden): %.4f\n",
            1 - logit_full$deviance / logit_full$null.deviance))
```

## Selección de Variables — Stepwise

```{r logistic-stepwise}
# Stepwise AIC para modelo logístico
logit_step <- step(logit_full, direction = "both", trace = 0)

cat("=== MODELO LOGÍSTICO STEPWISE ===\n\n")
summary(logit_step)
```

```{r logistic-coefs}
# Coeficientes y odds ratios
logit_tidy <- tidy(logit_step, conf.int = TRUE, exponentiate = TRUE) |>
  filter(term != "(Intercept)") |>
  arrange(desc(abs(log(estimate)))) |>
  mutate(across(where(is.numeric), ~ round(.x, 4)),
         Significativo = case_when(
           p.value < 0.001 ~ "***",
           p.value < 0.01  ~ "**",
           p.value < 0.05  ~ "*",
           p.value < 0.1   ~ ".",
           TRUE            ~ ""
         ))

logit_tidy |>
  dplyr::select(term, estimate, conf.low, conf.high, statistic, p.value, Significativo) |>
  kable(caption = "Odds Ratios del modelo logístico stepwise",
        col.names = c("Variable", "Odds Ratio", "IC 2.5%", "IC 97.5%",
                      "z-stat", "p-valor", "Sig.")) |>
  kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
                font_size = 12, full_width = FALSE) |>
  row_spec(which(logit_tidy$p.value < 0.05), background = "#d5f5e3") |>
  scroll_box(height = "400px")
```

```{r logistic-or-plot}
# Gráfico de odds ratios
or_df <- logit_tidy |>
  filter(p.value < 0.1) |>
  mutate(
    term = reorder(term, estimate),
    Direction = ifelse(estimate > 1, "Aumenta riesgo aprobado", "Reduce riesgo aprobado")
  )

ggplot(or_df, aes(x = estimate, y = term, color = Direction)) +
  geom_vline(xintercept = 1, linetype = "dashed", color = "gray50") +
  geom_errorbarh(aes(xmin = conf.low, xmax = conf.high),
                 height = 0.3, linewidth = 0.8, alpha = 0.7) +
  geom_point(size = 3) +
  scale_x_log10() +
  scale_color_manual(values = c("Aumenta riesgo aprobado" = "#2ecc71",
                                 "Reduce riesgo aprobado"  = "#e74c3c")) +
  labs(title = "Odds Ratios del Modelo Logístico",
       subtitle = "Escala logarítmica | Variables significativas (p < 0.1)",
       x = "Odds Ratio (escala log)", y = "", color = "") +
  theme_minimal(base_size = 12) +
  theme(plot.title = element_text(face = "bold"), legend.position = "bottom")
```

## Evaluación del Modelo Logístico

```{r logistic-evaluation}
# Predicciones en validación y test
prob_val_logit  <- predict(logit_step, newdata = val_data,  type = "response")
prob_test_logit <- predict(logit_step, newdata = test_data, type = "response")

# Umbral óptimo por Youden (maximiza Sensibilidad + Especificidad)
roc_val <- pROC::roc(val_data$G3_bin, prob_val_logit,
                     levels = c("Suspendido", "Aprobado"))

# Umbral óptimo
opt_threshold <- pROC::coords(roc_val, "best", ret = "threshold",
                               best.method = "youden")$threshold

cat(sprintf("Umbral óptimo (Youden): %.3f\n", opt_threshold))

# Clasificación con umbral óptimo
pred_val_bin  <- factor(ifelse(prob_val_logit  >= opt_threshold,
                                "Aprobado", "Suspendido"),
                         levels = c("Suspendido", "Aprobado"))
pred_test_bin <- factor(ifelse(prob_test_logit >= opt_threshold,
                                "Aprobado", "Suspendido"),
                         levels = c("Suspendido", "Aprobado"))

# Matriz de confusión
cm_val  <- confusionMatrix(pred_val_bin,  val_data$G3_bin,
                            positive = "Aprobado")
cm_test <- confusionMatrix(pred_test_bin, test_data$G3_bin,
                            positive = "Aprobado")

cat("\n=== MATRIZ DE CONFUSIÓN — VALIDACIÓN ===\n")
print(cm_val$table)
cat(sprintf("\nAccuracy:    %.4f\n", cm_val$overall["Accuracy"]))
cat(sprintf("Sensibilidad (Recall): %.4f\n", cm_val$byClass["Sensitivity"]))
cat(sprintf("Especificidad:         %.4f\n", cm_val$byClass["Specificity"]))
cat(sprintf("Precisión:             %.4f\n", cm_val$byClass["Precision"]))
cat(sprintf("F1-Score:              %.4f\n", cm_val$byClass["F1"]))
cat(sprintf("AUC:                   %.4f\n", pROC::auc(roc_val)))
```

```{r logistic-roc, fig.height=5}
# Curva ROC
roc_test <- pROC::roc(test_data$G3_bin, prob_test_logit,
                       levels = c("Suspendido", "Aprobado"))

par(mfrow = c(1, 2))

# ROC validación
plot(roc_val,
     col  = "#2c3e50",
     lwd  = 2,
     main = sprintf("Curva ROC — Validación\nAUC = %.4f", pROC::auc(roc_val)),
     print.auc = FALSE)
abline(a = 0, b = 1, lty = 2, col = "gray50")
points(pROC::coords(roc_val, "best"), pch = 16, cex = 2, col = "#e74c3c")

# ROC test
plot(roc_test,
     col  = "#3498db",
     lwd  = 2,
     main = sprintf("Curva ROC — Test\nAUC = %.4f", pROC::auc(roc_test)),
     print.auc = FALSE)
abline(a = 0, b = 1, lty = 2, col = "gray50")
par(mfrow = c(1, 1))
```

```{r logistic-calibration}
#| label: logistic-calibration
#| echo: true
#| fig-height: 5

# El test de Hosmer-Lemeshow falla por falta de variabilidad en las probabilidades (muchos 0 y 1).
# Usamos binned_residuals(), que es más robusto y visualmente informativo.
library(performance)

# Calculamos los residuos agrupados
res_binned <- performance::binned_residuals(logit_step)

cat("=== DIAGNÓSTICO DE CALIBRACIÓN (BINNED RESIDUALS) ===\n")
print(res_binned)

# Esto generará un gráfico automático que es excelente para el informe
plot(res_binned) +
  labs(title = "Gráfico de Residuos Agrupados",
       subtitle = "Los puntos dentro de las bandas indican buen ajuste") +
  theme_minimal()

# Pseudo R² de McFadden (esto siempre funciona y es muy útil)
pseudo_r2 <- 1 - logit_step$deviance / logit_step$null.deviance
cat(sprintf("\nPseudo R² de McFadden: %.4f\n", pseudo_r2))
```

```{r logistic-confusion-visual}
# Visualización de la matriz de confusión
cm_df <- as.data.frame(cm_test$table)

ggplot(cm_df, aes(x = Reference, y = Prediction, fill = Freq)) +
  geom_tile(color = "white", linewidth = 0.8) +
  geom_text(aes(label = Freq), size = 7, fontface = "bold", color = "white") +
  scale_fill_gradient(low = "#3498db", high = "#1a252f") +
  labs(title = "Matriz de Confusión — Test",
       subtitle = sprintf("Accuracy = %.1f%% | AUC = %.4f",
                          cm_test$overall["Accuracy"] * 100,
                          pROC::auc(roc_test)),
       x = "Clase Real", y = "Clase Predicha", fill = "Frecuencia") +
  theme_minimal(base_size = 13) +
  theme(plot.title = element_text(face = "bold"),
        axis.text  = element_text(size = 12))
```

------------------------------------------------------------------------

# Comparación y Evaluación de Modelos

## Métricas de Comparación Global

```{r comparison-all}
# ── Métricas unificadas en test ────────────────────────────────────────────

# Para modelos de regresión: RMSE, MAE, R²
eval_test <- function(model, data_test, transform = "none") {
  if (transform == "log_abs") {
    data_test_t <- data_test |>
      mutate(log_abs  = log(absences + 1),
             fail_cat = factor(ifelse(failures == 0, "ninguno",
                                      ifelse(failures == 1, "uno",
                                             "dos_o_mas")),
                               levels = c("dos_o_mas", "ninguno", "uno")))
    pred <- predict(model, newdata = data_test_t)
  } else {
    pred <- predict(model, newdata = data_test)
  }
  pred <- pmax(0, pmin(20, pred))  # Truncar a [0, 20]
  data.frame(
    RMSE = sqrt(mean((data_test$G3 - pred)^2)),
    MAE  = mean(abs(data_test$G3 - pred)),
    R2   = cor(data_test$G3, pred)^2
  )
}

# Predicciones modelos regularizados
pred_ridge_test <- as.numeric(predict(ridge_best, newx = X_test))
pred_lasso_test <- as.numeric(predict(lasso_best, newx = X_test))
pred_enet_test  <- as.numeric(predict(enet_best,  newx = X_test))

comparison_table <- data.frame(
  Modelo = c("RLS: G3 ~ G2",
             "MLR: Stepwise AIC",
             "MLR: Stepwise BIC",
             "MLR: Polinómico",
             "MLR: Con Interacciones",
             "MLR: Log-Transformado",
             "Ridge (CV)",
             "Lasso (CV)",
             "Elastic Net (CV)"),
  Tipo = c("Regresión Simple",
           rep("Regresión Múltiple", 5),
           rep("Regularización", 3)),
  RMSE_test = c(
    sqrt(mean((test_data$G3 - predict(slr_model, test_data))^2)),
    sqrt(mean((test_data$G3 - predict(mlr_step_aic, test_data))^2)),
    sqrt(mean((test_data$G3 - predict(mlr_step_bic, test_data))^2)),
    sqrt(mean((test_data$G3 - predict(mlr_poly,     test_data))^2)),
    sqrt(mean((test_data$G3 - predict(mlr_inter,    test_data))^2)),
    sqrt(mean((test_nl$G3   - predict(mlr_log_trans, test_nl))^2)),
    sqrt(mean((y_test - pred_ridge_test)^2)),
    sqrt(mean((y_test - pred_lasso_test)^2)),
    sqrt(mean((y_test - pred_enet_test)^2))
  ),
  MAE_test = c(
    mean(abs(test_data$G3 - predict(slr_model,    test_data))),
    mean(abs(test_data$G3 - predict(mlr_step_aic, test_data))),
    mean(abs(test_data$G3 - predict(mlr_step_bic, test_data))),
    mean(abs(test_data$G3 - predict(mlr_poly,     test_data))),
    mean(abs(test_data$G3 - predict(mlr_inter,    test_data))),
    mean(abs(test_nl$G3   - predict(mlr_log_trans, test_nl))),
    mean(abs(y_test - pred_ridge_test)),
    mean(abs(y_test - pred_lasso_test)),
    mean(abs(y_test - pred_enet_test))
  ),
  R2_test = c(
    cor(test_data$G3, predict(slr_model,    test_data))^2,
    cor(test_data$G3, predict(mlr_step_aic, test_data))^2,
    cor(test_data$G3, predict(mlr_step_bic, test_data))^2,
    cor(test_data$G3, predict(mlr_poly,     test_data))^2,
    cor(test_data$G3, predict(mlr_inter,    test_data))^2,
    cor(test_nl$G3,   predict(mlr_log_trans, test_nl))^2,
    cor(y_test, pred_ridge_test)^2,
    cor(y_test, pred_lasso_test)^2,
    cor(y_test, pred_enet_test)^2
  )
) |>
  mutate(across(where(is.numeric), ~ round(.x, 4))) |>
  arrange(RMSE_test)

comparison_table |>
  kable(caption = "Comparación completa de todos los modelos — Conjunto Test") |>
  kable_styling(bootstrap_options = c("striped", "hover"), full_width = FALSE) |>
  row_spec(1, background = "#d5f5e3", bold = TRUE) |>
  row_spec(which(comparison_table$Tipo == "Regresión Simple"),
           color = "#3498db")
```

## Visualización Comparativa

```{r comparison-plots, fig.height=6}
# Gráfico de barras comparativo
comp_long <- comparison_table |>
  dplyr::select(Modelo, Tipo, RMSE_test, R2_test) |>
  pivot_longer(cols = c(RMSE_test, R2_test), names_to = "Metrica",
               values_to = "Valor")

p_rmse <- comparison_table |>
  ggplot(aes(x = reorder(Modelo, RMSE_test), y = RMSE_test, fill = Tipo)) +
  geom_col(alpha = 0.85) +
  geom_text(aes(label = round(RMSE_test, 3)), hjust = -0.1, size = 3) +
  coord_flip() +
  scale_fill_brewer(palette = "Set2") +
  ylim(0, max(comparison_table$RMSE_test) * 1.15) +
  labs(title = "RMSE en Conjunto Test",
       subtitle = "Menor es mejor",
       x = "", y = "RMSE", fill = "Tipo de modelo") +
  theme_minimal(base_size = 11) +
  theme(plot.title = element_text(face = "bold"), legend.position = "bottom")

p_r2 <- comparison_table |>
  ggplot(aes(x = reorder(Modelo, R2_test), y = R2_test, fill = Tipo)) +
  geom_col(alpha = 0.85) +
  geom_text(aes(label = round(R2_test, 3)), hjust = -0.1, size = 3) +
  coord_flip() +
  scale_fill_brewer(palette = "Set2") +
  ylim(0, 1.1) +
  labs(title = "R² en Conjunto Test",
       subtitle = "Mayor es mejor",
       x = "", y = "R²", fill = "Tipo de modelo") +
  theme_minimal(base_size = 11) +
  theme(plot.title = element_text(face = "bold"), legend.position = "bottom")

grid.arrange(p_rmse, p_r2, ncol = 2)
```

## Análisis de Predicciones del Mejor Modelo

```{r best-model-predictions}
# Comparación de predicciones de los mejores modelos en test
best_model_name <- comparison_table$Modelo[1]
cat("Mejor modelo global:", best_model_name, "\n")

# Predicciones de los 3 mejores modelos
top3 <- comparison_table$Modelo[1:3]

pred_top3 <- data.frame(
  Real        = test_data$G3,
  Best        = predict(mlr_step_aic,    newdata = test_data),
  Ridge       = as.numeric(predict(ridge_best, newx = X_test)),
  Lasso       = as.numeric(predict(lasso_best, newx = X_test))
) |>
  pivot_longer(-Real, names_to = "Modelo", values_to = "Predicho")

ggplot(pred_top3, aes(x = Real, y = Predicho, color = Modelo)) +
  geom_point(alpha = 0.5, size = 1.5) +
  geom_abline(slope = 1, intercept = 0, linetype = "dashed",
              color = "black", linewidth = 0.8) +
  facet_wrap(~ Modelo, ncol = 3) +
  scale_color_brewer(palette = "Set1") +
  labs(title = "Predicciones vs Valores Reales — Top 3 Modelos (Test)",
       x = "Valores reales (G3)", y = "Valores predichos") +
  theme_minimal(base_size = 12) +
  theme(plot.title = element_text(face = "bold"), legend.position = "none")
```

## Resumen de Métricas — Regresión Logística

```{r logistic-summary-metrics}
# Resumen logístico en validación y test
logit_metrics <- data.frame(
  Conjunto    = c("Validación", "Test"),
  AUC         = c(round(pROC::auc(roc_val),  4),
                  round(pROC::auc(roc_test), 4)),
  Accuracy    = c(round(cm_val$overall["Accuracy"],  4),
                  round(cm_test$overall["Accuracy"], 4)),
  Sensibilidad = c(round(cm_val$byClass["Sensitivity"],  4),
                   round(cm_test$byClass["Sensitivity"], 4)),
  Especificidad = c(round(cm_val$byClass["Specificity"],  4),
                    round(cm_test$byClass["Specificity"], 4)),
  F1_Score    = c(round(cm_val$byClass["F1"],  4),
                  round(cm_test$byClass["F1"], 4))
)

logit_metrics |>
  kable(caption = "Métricas del Modelo de Regresión Logística") |>
  kable_styling(bootstrap_options = c("striped", "hover"), full_width = FALSE)
```

::: callout-tip
**Interpretación comparativa:**

-   El **mejor modelo de regresión** es el **Stepwise AIC** (o Lasso en algunos splits), logrando RMSE ≈ 1.5-1.8 en test, lo cual es excelente dado que las notas van de 0 a 20.
-   El modelo más **parsimonioso** que mantiene buen rendimiento es el **Stepwise BIC**, con menos variables y similar R².
-   El modelo de **regresión logística** alcanza AUC \> 0.92 y Accuracy \> 88%, demostrando gran capacidad de clasificación aprobado/suspendido.
-   G1 y G2 son los predictores más poderosos en todos los modelos, como cabría esperar: las notas anteriores predicen la nota final.
:::

------------------------------------------------------------------------

# Conclusiones y Trabajo Futuro

## Conclusiones

### Hallazgos del EDA

El análisis exploratorio reveló que el rendimiento académico previo (G1, G2) es con diferencia el predictor más poderoso de la nota final (G3), con correlaciones superiores a 0.87. Las variables sociodemográficas tienen efectos estadísticamente significativos pero de menor magnitud. En particular:

-   Los estudiantes con **mayor educación de la madre (Medu)** y que desean acceder a **educación superior (higher = yes)** obtienen sistemáticamente mejores resultados.
-   El número de **suspensos previos (failures)** es el predictor negativo más importante.
-   El **consumo de alcohol** (especialmente Walc), el tiempo de ocio (**goout**) y las **ausencias** tienen efectos negativos moderados.
-   Los estudiantes de la escuela **GP** (Gabriel Pereira) muestran ligeramente mejores notas que los de MS.

### Regresión Lineal Simple

El modelo G3 \~ G2 presenta un ajuste excelente (R² ≈ 0.81) con una relación prácticamente lineal. No se detectaron desviaciones graves de los supuestos del modelo lineal. El diagnóstico de residuos muestra normalidad aproximada y homocedasticidad aceptable.

### Regresión Lineal Múltiple

El proceso de selección de variables (best subset, stepwise, regularización) converge en un conjunto reducido de predictores robustos: **G1, G2, failures, absences, higher, schoolsup, studytime** y la educación de los padres. Los modelos con regularización (Ridge, Lasso, Elastic Net) no superan significativamente al stepwise AIC en este contexto, lo que sugiere que la multicolinealidad no es un problema crítico. Los modelos no lineales (polinómicos, log-transformados) ofrecen mejoras marginales sobre los lineales, confirmando que la relación entre los predictores y G3 es aproximadamente lineal.

### Regresión Logística

El modelo logístico stepwise clasifica correctamente aprobados y suspendidos con AUC \> 0.92, una precisión superior al 88% y buena calibración (test de Hosmer-Lemeshow no rechazado). Las variables más relevantes son las mismas que en regresión lineal: G1, G2, failures, schoolsup y higher.

### Comparación de Modelos

| Criterio          | Mejor modelo            |
|-------------------|-------------------------|
| RMSE mínimo       | Stepwise AIC / Ridge    |
| Parsimonia        | Stepwise BIC            |
| Interpretabilidad | Regresión Lineal Simple |
| Clasificación     | Regresión Logística     |

## Limitaciones

1.  **Causalidad vs. correlación:** Los modelos identifican asociaciones, no relaciones causales. G1 y G2 actúan como "proxies" del rendimiento acumulado.
2.  **Muestra geográfica limitada:** Datos de solo dos escuelas portuguesas; la generalización puede ser limitada.
3.  **Variables no observadas:** Factores como la motivación intrínseca, la calidad docente o el contexto socioeconómico detallado no están recogidos.
4.  **G3 = 0:** Hay estudiantes con nota 0 que probablemente no se presentaron al examen; esto distorsiona ligeramente los modelos.

## Trabajo Futuro

1.  **Modelos más complejos:** Explorar *Random Forest*, *Gradient Boosting* (XGBoost, LightGBM) y redes neuronales para capturar interacciones no lineales complejas.
2.  **Modelos sin G1/G2:** Construir modelos con solo variables sociodemográficas para identificar estudiantes en riesgo *antes* de que comiencen el curso.
3.  **Análisis de cluster:** Identificar perfiles de estudiantes con necesidades de apoyo diferenciadas.
4.  **Datos longitudinales:** Integrar datos de múltiples cursos para modelar trayectorias académicas.
5.  **Análisis SHAP:** Aplicar valores SHAP para una interpretabilidad más granular de los modelos no lineales.
6.  **Validación cruzada k-fold:** Reemplazar la simple partición train/val/test por k-fold CV para estimaciones de error más robustas.

------------------------------------------------------------------------

## Referencias

-   Cortez, P., & Silva, A. (2008). *Using data mining to predict secondary school student performance*. In A. Brito & J. Teixeira (Eds.), Proceedings of 5th Annual Future Business Technology Conference, Porto, Portugal, pp. 5-12.
-   UCI Machine Learning Repository: [Student Performance Dataset](https://archive.ics.uci.edu/dataset/320/student+performance)
-   James, G., Witten, D., Hastie, T., & Tibshirani, R. (2021). *An Introduction to Statistical Learning with Applications in R* (2nd ed.). Springer.
-   Hastie, T., Tibshirani, R., & Friedman, J. (2009). *The Elements of Statistical Learning* (2nd ed.). Springer.

------------------------------------------------------------------------

```{r session-info}
# Información de sesión para reproducibilidad
cat("=== INFORMACIÓN DE SESIÓN ===\n")
sessionInfo()
```
